# Core data pipeline (Phases 1-5)
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet support
requests>=2.31
rapidfuzz>=3.6         # player name matching (Phase 4)
scipy>=1.12            # Dixon-Coles (Phase 6)

# Lineup scraping (Phase 2)
understatapi>=0.5

# Dev tooling
pytest>=8.0
ruff>=0.4

# Added in later phases, kept here as a record of what is coming:
autogluon.tabular>=1.1  # Phase 6 models (~700 MB installed)
typing_extensions       # autogluon needs it but does not declare it
#   streamlit>=1.35          (Phase 8)
