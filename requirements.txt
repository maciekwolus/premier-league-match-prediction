# Everything needed to build the data, run the tests and open the report.
#
# Deliberately small and quick to install. The one heavy dependency - AutoGluon, around
# 700 MB - lives in requirements-models.txt instead, because nothing here needs it: the
# full test suite passes with it absent, and `compare --fast` skips it.

# Core data pipeline
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet support
requests>=2.31
rapidfuzz>=3.6         # player name matching
scipy>=1.12            # fits Dixon-Coles, the model the report uses

# Lineup scraping
understatapi>=0.5

# Report
streamlit>=1.35

# Dev tooling
pytest>=8.0
ruff>=0.4
