# Everything needed to run the report, run the tests, and build most of the pipeline.
#
# Deliberately small, quick to install, and free of hard version pins - which is what
# makes it safe to deploy and fast in CI. The two dependencies that are neither live in
# requirements-full.txt, and nothing here needs them: all 441 tests pass with both absent.

# Core data pipeline
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet support
requests>=2.31
rapidfuzz>=3.6         # player name matching
scipy>=1.12            # fits Dixon-Coles, the model the report uses

# Report
streamlit>=1.35

# Dev tooling
pytest>=8.0
ruff>=0.4
