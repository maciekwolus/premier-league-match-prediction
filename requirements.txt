# Core data pipeline (Phases 1-5)
pandas>=2.2
numpy>=1.26
pyarrow>=15.0          # parquet support
requests>=2.31
rapidfuzz>=3.6         # player name matching (Phase 4)
scipy>=1.12            # Dixon-Coles (Phase 6)

# Lineup scraping (Phase 2)
understatapi>=0.5

# Dev tooling
pytest>=8.0
ruff>=0.4

# Models (Phase 6). This is the bulk of the install - around 700 MB.
# Skip it if you only need the data pipeline; `compare --fast` runs without it.
autogluon.tabular>=1.1
typing_extensions      # autogluon imports it without declaring the dependency

# Report (Phase 8)
streamlit>=1.35
